#pragma once

#include <string>

const int VIEWPORT_WIDTH = 800;
const int VIEWPORT_HEIGHT = 800;
const int FRAMERATE_LIMIT = 60;
const char* APP_TITLE = "fleng";

const std::string SHADERS_DIR = "shaders/";

const int INITIAL_MARCH_ITERATIONS = 200;

const float CAM_MATRIX_SIZE = 0.001;
const float CAM_INIT_SPEED = 0.2;
const float CAM_ROT_ANGLE = 0.05;
